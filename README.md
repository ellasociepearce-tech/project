# CISC 121 Final Project Ella Pearce: Playlist Vibe Builder  
## Playlist vibe builder is used to organize a list of songs by sorting them based on the user's input. This helps create a playlist based on a specific characteristic, making it more efficient for the user's vibe.  
## The algorithm I used is merge sort. Merge sort has a time complexity of O(nlog⁡n), making it great for organizing playlists large in size. It is also stable, so songs with the same value will keep their original order. 
## <img width="1456" height="835" alt="image" src="https://github.com/user-attachments/assets/bb936c80-756a-47a0-8a14-69f16fd1f234" />
## - Decomp: Take in a list of songs, user chooses a sorting key, divide the playlist into smaller sublists, recursively sort each sublist, merge the sorted sublists back together, output the final sorted playlist and display the steps.
## - Pattern recognition: Repeatedly split the list into halves until single elements remain, compare values when merging, the same process is used at every level of the recursion. 
## - Abstraction: Only the selected characteristic is used for sorting ignoring the other details, key steps like splitting and merging are shown while ignoring the rest of the details not involved in the process.
## - Algorithmic Thinking: Input: User provides a playlist and chooses a sorting key. Processing: Merge sort recursively splits and merges the playlist based on the chosen characteristic. Output: A sorted playlist is given to the user. GUI: Show the list is split into smaller parts and comparisons during merging, animate how elements move into their sorted positions, display the final ordered playlist. 
## flowchart: 
## <img width="430" height="790" alt="image" src="https://github.com/user-attachments/assets/f53c1852-de95-4078-86ea-946eb1a77e72" />
## Steps to Run (local) + requirements.txt 
## https://ellapearce-cisc-121-final-project.hf.space/?logs=build&__theme=system&deep_link=iCJUMidGTSA 
## I first tested the app with a correctly formatted CSV file that has all the required data included. I also provided a valid key, it was expected to give me a sorted list in descending order with the required merge sort step by step. This gave exactly what I needed confirming the base case of the program works correctly. I then moved on to handling edge cases with my first one being an invalid or empty file was given. The expected input was for the system to crash and display an error message about the code. In turn I provided code so the system didn’t crash and an error message asking for a valid CSV file with all required columns and information. I then handled the edgecase where the user input a valid key but it had uppercase letters or weird spaces. This would make the system crash again because a valid key was not chosen. Instead the code stripped the spaces and made the letters lowercase so it would correctly organise the playlist with the key provided. The last edge case I tested was if a random word was submitted in the key value. I tested this by putting in “hello”. Without handling this the system would have crashed and not run providing an error message about my code. In turn it rejects the input and asks the user to use a valid key, repeated until “energy” or “duration” is entered. <img width="1659" height="692" alt="Screenshot_15-4-2026_155751_huggingface co" src="https://github.com/user-attachments/assets/59cd3165-dcfa-46a5-929e-85c8bc9a2f78" /> <img width="1665" height="535" alt="Screenshot_15-4-2026_155715_huggingface co" src="https://github.com/user-attachments/assets/3053b381-a33f-4a31-8c5a-f8a378798e22" /> <img width="1495" height="871" alt="Screenshot_15-4-2026_155727_huggingface co" src="https://github.com/user-attachments/assets/b8358e32-9e43-4f19-a099-86b79eb384fa" />
## Author: Ella Pearce
## Gradio was used to create the graphics and visuals for the user interface and Hugging Face Spaces was used to create the final interactive app. ChatGPT was used for brainstorming and coding, gradio/visual help, debugging help, and identifying edge cases. All final code, testing decisions, and project organization were reviewed and revised by me.


